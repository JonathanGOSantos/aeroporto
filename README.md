O projeto utiliza NodeJS e Vite. Para executar, é necessário ter o node instalado.
Com o node instalado, rode: `npm install` e depois `npm run dev`.
Para compilar o projeto para deploy, `npm run build`.
